# AfriShield weekly site-health report — 2026-09-14

**Run type:** primary weekly check (Monday)
**Overall status: AMBER**

## Checks

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | Live routes | **INFO — not verified** | This session's outbound HTTPS egress is blocked entirely by org network policy: `https://afrishield-ai-seo.vercel.app/`, `https://afrishieldai.com/`, and even a neutral control host (`https://example.com/`) all returned `connect_rejected` / 403 at the agent-proxy/gateway level via both `curl` and `WebFetch` (`curl: (56) CONNECT tunnel failed, response 403`; proxy status confirms `connect_rejected` for all three hosts). This is the same environment gap first documented in the 2026-08-06 report and has not been fixed since — see "Action needed." Not evidence the site is down. |
| 2 | Build integrity | **PASS** | `npm install` (409 packages, ~27s) then `npx next build` completed with no errors (TypeScript check passed, 44/44 static pages generated). All 23 blog posts in `lib/posts.ts` emitted a route: `/blog/ai-search-visibility-study`, `/blog/ai-seo-vs-traditional-seo`, `/blog/answer-engine-optimization`, `/blog/content-marketing-for-small-business`, `/blog/enterprise-geo-launch-africa`, `/blog/google-business-profile-optimization`, `/blog/how-long-does-seo-take`, `/blog/how-to-appear-on-google-maps`, `/blog/how-to-do-keyword-research`, `/blog/how-to-get-hotel-cited-by-chatgpt`, `/blog/how-to-get-more-website-traffic`, `/blog/how-to-get-my-business-on-google`, `/blog/how-to-improve-google-ranking`, `/blog/how-to-rank-on-chatgpt`, `/blog/how-to-reduce-ota-commission`, `/blog/is-seo-worth-it`, `/blog/local-seo-for-small-business`, `/blog/seo-vs-google-ads`, `/blog/small-business-seo`, `/blog/top-geo-ai-seo-agencies-africa-2026`, `/blog/what-ai-seo-actually-does`, `/blog/what-is-schema-markup`, `/blog/what-seo-actually-costs`, plus `/`, `/about`, `/ai-visibility-check`, `/api/call-summary`, `/api/contact`, `/blog`, `/case-studies`, `/contact`, `/for-lodges-hotels`, `/for-safari-operators`, `/for-tourism-hospitality`, `/geo-services`, `/how-it-works`, `/llms.txt`, `/pricing`, `/resources/ai-visibility-checklist`, `/sitemap.xml`, `/solutions`, `/thank-you`. |
| 3 | Content schedule | **PASS (on schedule), queue flagged LOW** | DUE (campaign dates ≤ 2026-09-14): 15 of 15 (all dates 2026-08-03 through 2026-08-23 have passed). DONE (posts in `lib/posts.ts` with `published` ≥ 2026-08-03): 19. Site is ahead of the campaign schedule, not behind. `content-queue.md` has only **1** row with status `queued` (row 9, "what is local seo" — parked since 2026-08-18 over an unresolved angle-duplication flag against `local-seo-for-small-business`, now 5+ skipped runs old per the file's own skip notes). That is below the file's stated 4-row low-queue floor and below its own 6-row top-up floor. Everything else in the pool (rows 16–31) is still `candidate`, awaiting a DataForSEO pass — the file itself already flags this as a risk of running dry. |
| 4 | Internal consistency | **1 mismatch found (fixed on this branch)** | All 23 posts in `lib/posts.ts` have a matching `app/blog/<slug>/page.tsx` — no missing pages. However, the post `ai-search-visibility-study` (published 2026-08-28, primary keyword "ai search visibility for african businesses") had **no corresponding row in `used-keywords.md`** — it was published without being logged, breaking the file's own "one row per claimed keyword, ever" invariant and making it invisible to the duplicate-keyword check the poster and queue-keeper rely on. **Safe fix applied on this branch:** added the missing row (cluster B — GEO / AI-search, `/blog/ai-search-visibility-study`, claimed 2026-08-28, Live) in chronological position. All other 22 published slugs are correctly logged. |
| 5 | Contact endpoint | **INFO — not verified** | Same network block as Check 1 — could not GET `/api/contact` live. Build output confirms the route exists and compiles (`ƒ /api/contact`, dynamic), so it should deploy correctly, but live availability was not confirmed this run. |
| 6 | Performance signal | **INFO — not verified** | Same network block as Check 1 — could not measure TTFB/byte size for `/`. |

## Action needed

1. **The network/environment gap from 2026-08-06 is still open, six weeks later.** This routine has had zero outbound HTTPS egress in every run since then — Checks 1, 5, and 6 have not produced a real PASS/FAIL in that entire span, only "unknown." Someone with access to this environment's network policy should allow egress to `afrishield-ai-seo.vercel.app` and `afrishieldai.com` for this scheduled routine, or confirm general web egress is intentionally off and this routine needs a different mechanism (e.g. an external uptime monitor) for Checks 1/5/6 instead.
2. **Content queue is down to 1 `queued` row**, and it's a row that's been skipped 5+ times in a row over an unresolved duplication flag. Needs a decision (narrow the angle or retire row 9) plus a DataForSEO pass on the 14 `candidate` rows (16–31) to refill the pool before the next scheduled post has nothing to draw from.
3. **Fixed this run:** `ai-search-visibility-study` was published (2026-08-28) without a matching `used-keywords.md` entry. Added on branch `health/report-2026-09-14`. Recommend checking why the publish flow (or whoever ran it) skipped the "append to used-keywords.md" step for this one post, since the file's own rules require it every time.

## Not covered by this routine

Keyword rank positions, AI-answer presence (ChatGPT/Claude/Perplexity/Gemini), full Lighthouse scores, and true end-to-end contact-form submission need a SERP/DataForSEO connector or a headless browser (Playwright) that this cloud routine does not have; they are tracked separately in interactive sessions and via the planned Playwright + DataForSEO setup.
