# Team photos

Drop Benjamin's portrait here as **`benjamin-njock.jpg`** (the professional
desk photo). The About page (`/about`) references `/team/benjamin-njock.jpg` and
will display it automatically — no code change needed. Until the file exists,
the page shows a branded "BN" placeholder rather than a stock stand-in.

Recommended: portrait orientation, ~800×1000 or larger, under ~300 KB after
compression.
